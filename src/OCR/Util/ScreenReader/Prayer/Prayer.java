package OCR.Util.ScreenReader.Prayer;

public class Prayer {
    /**
     * The job of this class is to parse the prayer points given a file name. It'll return an array where the first
     * element is the number of prayer points left. The second element is the Prayer Level * 10
     */
}
